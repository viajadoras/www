---
import { Image } from 'astro:assets';
import heroMainImg from '../assets/images/hero-main.png';
import AppStoreBadges from './AppStoreBadges.astro';
---
<section class="relative min-h-[92vh] flex items-center overflow-hidden bg-neutral-50">

  <div class="absolute inset-0 opacity-[0.03]">
    <svg width="100%" height="100%">
      <defs>
        <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
          <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" class="text-primary" stroke-width="0.5"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#grid)" />
    </svg>
  </div>

  <div class="absolute top-0 right-0 w-[600px] h-[600px] bg-gradient-radial from-secondary/20 to-transparent blur-3xl"></div>
  <div class="absolute bottom-0 left-0 w-[500px] h-[500px] bg-gradient-radial from-primary/10 to-transparent blur-3xl"></div>

  <div class="container mx-auto px-4 sm:px-6 lg:px-12 relative z-10 py-12 lg:py-32">
    <div class="flex flex-col lg:grid lg:grid-cols-[0.9fr_1.1fr] gap-12 lg:gap-12 items-center max-w-[1800px] mx-auto">

      <!-- Texto - ordem 1 em todos os tamanhos -->
      <div class="space-y-10 max-w-2xl order-1 lg:row-span-2 lg:self-center">

        <div class="space-y-6">
          <h1 class="font-display text-[3.5rem] sm:text-[4.5rem] lg:text-[5.5rem] xl:text-[6.5rem] font-bold leading-[0.95] tracking-[-0.03em] text-neutral-900">
            Viaje com
            <span class="relative inline-block mt-2">
              <span class="block bg-gradient-to-r from-primary via-primary-400 to-primary bg-clip-text text-transparent bg-[length:200%_auto] animate-shimmer">
                liberdade
              </span>
              <svg class="absolute -bottom-2 left-0 w-full opacity-50 text-primary" viewBox="0 0 200 12" xmlns="http://www.w3.org/2000/svg">
                <path d="M0 6 Q 50 0, 100 6 T 200 6" stroke="currentColor" stroke-width="3" fill="none" stroke-linecap="round"/>
              </svg>
            </span>
            <span class="text-neutral-900">e</span>
            <span class="relative inline-block">
              <span class="block bg-gradient-to-r from-primary via-primary-400 to-primary bg-clip-text text-transparent bg-[length:200%_auto] animate-shimmer" style="animation-delay: 0.5s">
                companhia
              </span>
              <svg class="absolute -bottom-2 left-0 w-full opacity-50 text-primary" viewBox="0 0 200 12" xmlns="http://www.w3.org/2000/svg">
                <path d="M0 6 Q 50 0, 100 6 T 200 6" stroke="currentColor" stroke-width="3" fill="none" stroke-linecap="round"/>
              </svg>
            </span>
          </h1>
        </div>

        <div class="relative pl-6 border-l-4 border-primary">
          <p class="text-xl sm:text-2xl text-neutral-600 leading-relaxed max-w-xl font-light">
            Conheca mulheres, encontre companhia e viaje com mais confianca. Para <strong class="font-semibold text-neutral-900">qualquer mulher que viaja</strong>, do jeito que fizer sentido para voce.
          </p>
        </div>

        <!-- Botões no desktop (dentro do bloco de texto) -->
        <AppStoreBadges class="hidden lg:flex flex-row gap-4 pt-4 items-center" imgClass="h-20 w-auto" />
      </div>

      <!-- Imagem - ordem 2 no mobile, ao lado no desktop -->
      <div class="relative w-full lg:h-[700px] flex items-center justify-center order-2">
        <!-- Main Hero Image -->
        <div class="relative w-full h-[450px] lg:h-full rounded-3xl overflow-hidden shadow-2xl shadow-primary/20 group">
          <Image
            src={heroMainImg}
            alt="Grupo diverso de mulheres brasileiras viajando juntas no litoral"
            class="h-full w-full object-cover object-[98%_top] transition-transform duration-1000 group-hover:scale-105"
            loading="eager"
          />
          
          <!-- Gradient Overlay for text readability if needed, subtle -->
          <div class="absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent opacity-60"></div>
        </div>

        <!-- Floating Badge -->
        <div class="absolute bottom-8 left-8 bg-white/95 backdrop-blur-md px-5 py-3 rounded-2xl shadow-lg border border-white/50 flex items-center gap-3 animate-fade-in-up">
          <div class="w-10 h-10 rounded-full bg-success/10 flex items-center justify-center">
            <svg class="w-6 h-6 text-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <div>
            <div class="text-sm font-bold text-neutral-900">Comunidade verificada</div>
            <div class="text-xs text-neutral-500">Conexoes reais entre mulheres</div>
          </div>
        </div>
      </div>

      <!-- Botões no mobile (após a imagem) -->
      <AppStoreBadges class="flex lg:hidden flex-row gap-3 items-center order-3 w-full justify-center" imgClass="w-36 sm:w-44 h-auto" />

    </div>
  </div>

  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 z-20 hidden lg:block">
    <a href="#como-funciona" class="flex flex-col items-center gap-2 text-neutral-400 hover:text-primary transition-colors group">
      <span class="text-xs font-medium uppercase tracking-wider">Explore</span>
      <div class="w-6 h-10 border-2 border-current rounded-full flex items-start justify-center p-1.5">
        <div class="w-1.5 h-1.5 bg-current rounded-full animate-bounce"></div>
      </div>
    </a>
  </div>
</section>
